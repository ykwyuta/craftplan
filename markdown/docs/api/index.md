# API Reference

Craftplan exposes a [JSON:API](https://jsonapi.org)-compliant REST API. All endpoints require a Bearer
token; generate one in
[Settings → API Keys](/craftplan/docs/settings/)
(tokens use the `cpk_` prefix).

Scalar API Reference